// NB: I couldn't find the right combination of exclude/include in tsconfig.json to make this work, so 
// I had to add this import and it seems that vscode is just fine.  Absolutely no idea why this works.
import "@types/jest";
